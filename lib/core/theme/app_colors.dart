import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary
  static const Color primary = Color(0xFFD4740A);
  static const Color primaryLight = Color(0xFFFFF3E6);

  // Secondary
  static const Color secondary = Color(0xFF2E5090);

  // Semantic
  static const Color expense = Color(0xFFE74C3C);
  static const Color income = Color(0xFF2ECC71);

  // Background
  static const Color background = Color(0xFFFFF8F0);
  static const Color surface = Colors.white;

  // Text
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textHint = Color(0xFFBDBDBD);

  // Other
  static const Color divider = Color(0xFFE0E0E0);
  static const Color error = Color(0xFFE74C3C);
}
